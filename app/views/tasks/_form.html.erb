<%= form_with(model: @task, local: true) do |form| %>
  <%= form.label :name, t('task.column.name') %>
  <%= form.text_field :name %>
  <%= form.label :content, t('task.column.content') %>
  <%= form.text_area :content %>
  <%= form.submit t('task.submit') %>
<% end %>
