<%= form_with(model: @task, local: true) do |form| %>
  <%= form.label :name %>
  <%= form.text_field :name %>
  <%= form.label :content %>
  <%= form.text_area :content %>
  <%= form.submit "登録" %>
<% end %>
